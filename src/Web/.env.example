# No environment variable is required for normal local development.
# Vite proxies /api to the Visual Studio backend.
#
# Only set this when the API is hosted somewhere else:
# VITE_API_URL=http://localhost:57071/api/v1
